;;; These are examples of how to define values in the Scheme Computer Language.
;;; To define an absolute value is the line 'Abs'
;;;Example
(define (abs x) (if (< x 0) (- x) x))
;;; After this simple line of code, type in a number, negitive or positive.
(abs 6) 
;;; What will come out is the numbers absolute value if everything is typed correctly.
(abs - 10)
;;; This will show this line 
10
